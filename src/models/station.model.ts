export interface Country {
  name: string;
  value: number;
}

export interface City {
  name: string;
  value: number;
}

export interface Station {
  name: string;
  value: number;
}

export interface HsCodeModel {
  id: number;
  code: string;
  cargo: string;
}
