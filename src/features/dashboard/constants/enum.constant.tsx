// Contract Status Enum
export enum ContractStatus {
  verified,
  unverified,
  empty,
}

// Roles Enum
export enum Roles {
  lawyer,
  accountant,
  buyer_manager,
  commercial_manager,
  commercial_director,
}

// Invoice Status Enum
export enum InvoiceStatus {
  waiting,
  approved,
  reject,
}
